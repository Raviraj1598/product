
  # Admin panel for store

  This is a code bundle for Admin panel for store. The original project is available at https://www.figma.com/design/WCIa5zha2OY84KgVFPgE7c/Admin-panel-for-store.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  # product
